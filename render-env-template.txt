# Render Environment Variables Template
# Add these in your Render service dashboard -> Environment tab

# Database
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/doghub?retryWrites=true&w=majority

# JWT Secrets (Generate strong random strings - 64+ characters recommended)
USER_ACCESS_TOKEN_SECRET=your_super_secret_user_access_token_key_here_make_it_long_and_random
USER_REFRESH_TOKEN_SECRET=your_super_secret_user_refresh_token_key_here_make_it_long_and_random
ADMIN_ACCESS_TOKEN_SECRET=your_super_secret_admin_access_token_key_here_make_it_long_and_random

# Cloudinary (for image uploads)
CLOUDINARY_CLOUD_NAME=your_cloudinary_cloud_name
CLOUDINARY_API_KEY=your_cloudinary_api_key
CLOUDINARY_API_SECRET=your_cloudinary_api_secret

# Razorpay (payment gateway)
RAZORPAY_KEY_ID=rzp_test_your_key_id_here
RAZORPAY_KEY_SECRET=your_razorpay_secret_key_here

# Server Configuration
PORT=10000
NODE_ENV=production

# Frontend URL (for CORS)
FRONTEND_URL=https://your-app-name.vercel.app

# Instructions:
# 1. Go to your Render service dashboard
# 2. Click on "Environment" tab
# 3. Add each variable above with its actual value
# 4. Don't include quotes around the values
# 5. Redeploy your service after adding variables

# To generate JWT secrets, you can use:
# node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"